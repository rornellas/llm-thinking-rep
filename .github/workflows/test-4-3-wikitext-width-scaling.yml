name: Modal MoE — Test 4.3 WikiText width scaling

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/test-4-3-wikitext-width-scaling.yml"

permissions:
  contents: write

concurrency:
  group: modal-moe-test-4-3-width-scaling
  cancel-in-progress: false

jobs:
  width-scaling:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      PYTHONUNBUFFERED: "1"
      OMP_NUM_THREADS: "4"
      OPENBLAS_NUM_THREADS: "4"
      MKL_NUM_THREADS: "4"
      NUMEXPR_NUM_THREADS: "4"
      TOKENIZERS_PARALLELISM: "false"
      HF_HUB_DISABLE_XET: "1"
      HF_HOME: /tmp/huggingface

    steps:
      - name: Checkout experiment
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Register run
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p runs
          python - <<'PY'
          import json
          import os
          from datetime import datetime, timezone
          from pathlib import Path

          payload = {
              "workflow": "Modal MoE — Test 4.3 WikiText width scaling",
              "run_id": int(os.environ["GITHUB_RUN_ID"]),
              "run_attempt": int(os.environ["GITHUB_RUN_ATTEMPT"]),
              "status": "running",
              "commit": os.environ["GITHUB_SHA"],
              "started_at": datetime.now(timezone.utc).isoformat(),
          }
          Path("runs/test-4-3-latest.json").write_text(
              json.dumps(payload, indent=2) + "\n", encoding="utf-8"
          )
          PY
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add runs/test-4-3-latest.json
          git commit -m "chore: register Test 4.3 run ${GITHUB_RUN_ID}"
          git pull --rebase origin main
          git push origin HEAD:main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install --no-cache-dir --index-url https://download.pytorch.org/whl/cpu "torch>=2.3"
          python -m pip install --no-cache-dir "numpy>=2.0" "datasets>=4.0" "tokenizers>=0.20"

      - name: Prepare deterministic WikiText-2 BPE corpus
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p data/test-4-3 results/test-4-3/latest
          python - <<'PY'
          import json
          from pathlib import Path
          import numpy as np
          from datasets import load_dataset
          from tokenizers import Tokenizer, decoders, models, pre_tokenizers, trainers

          root = Path("data/test-4-3")
          dataset = load_dataset("Salesforce/wikitext", "wikitext-2-raw-v1")
          train_lines = [row["text"] for row in dataset["train"] if row["text"].strip()]
          validation_lines = [row["text"] for row in dataset["validation"] if row["text"].strip()]
          tokenizer = Tokenizer(models.BPE(unk_token="<unk>"))
          tokenizer.pre_tokenizer = pre_tokenizers.ByteLevel(add_prefix_space=False)
          tokenizer.decoder = decoders.ByteLevel()
          tokenizer.train_from_iterator(
              train_lines,
              trainer=trainers.BpeTrainer(
                  vocab_size=2048,
                  min_frequency=2,
                  special_tokens=["<unk>"],
                  show_progress=False,
              ),
          )

          def encode(lines):
              ids = []
              for line in lines:
                  ids.extend(tokenizer.encode(line + "\n").ids)
              return np.asarray(ids, dtype=np.int32)

          train = encode(train_lines)
          validation = encode(validation_lines)
          np.save(root / "train.npy", train)
          np.save(root / "validation.npy", validation)
          manifest = {
              "train_path": str(root / "train.npy"),
              "validation_path": str(root / "validation.npy"),
              "vocab_size": tokenizer.get_vocab_size(),
              "dataset": "Salesforce/wikitext",
              "subset": "wikitext-2-raw-v1",
          }
          (root / "manifest.json").write_text(
              json.dumps(manifest, indent=2) + "\n", encoding="utf-8"
          )
          Path("results/test-4-3/latest/dataset_stats.json").write_text(
              json.dumps(
                  {
                      **manifest,
                      "train_tokens": int(train.size),
                      "validation_tokens": int(validation.size),
                  },
                  indent=2,
              ) + "\n",
              encoding="utf-8",
          )
          PY
          test -s data/test-4-3/manifest.json

      - name: Validate source
        shell: bash
        run: |
          set -euo pipefail
          python -m py_compile experiments/test_4_3_wikitext_width_scaling.py

      - name: Run paired width-scaling curve
        shell: bash
        run: |
          set -euo pipefail
          python experiments/test_4_3_wikitext_width_scaling.py \
            --source experiments/test_2_0_modal_moe_trainability.py \
            --manifest data/test-4-3/manifest.json \
            --output-dir results/test-4-3/latest \
            --seed 44300 \
            --steps 300 \
            --threads 4 \
            2>&1 | tee test-4-3.log

      - name: Publish verdict
        if: always()
        shell: bash
        run: |
          [[ -f results/test-4-3/latest/VERDICT.md ]] && cat results/test-4-3/latest/VERDICT.md >> "$GITHUB_STEP_SUMMARY" || true

      - name: Persist result
        if: always()
        shell: bash
        env:
          RUN_STATUS: ${{ job.status }}
        run: |
          set -euo pipefail
          mkdir -p results/test-4-3/latest runs
          [[ -f test-4-3.log ]] && cp test-4-3.log results/test-4-3/latest/
          python - <<'PY'
          import json
          import os
          from datetime import datetime, timezone
          from pathlib import Path

          marker = Path("runs/test-4-3-latest.json")
          payload = json.loads(marker.read_text(encoding="utf-8")) if marker.exists() else {}
          payload.update({
              "run_id": int(os.environ["GITHUB_RUN_ID"]),
              "run_attempt": int(os.environ["GITHUB_RUN_ATTEMPT"]),
              "status": os.environ["RUN_STATUS"],
              "finished_at": datetime.now(timezone.utc).isoformat(),
          })
          marker.write_text(json.dumps(payload, indent=2) + "\n", encoding="utf-8")
          Path("results/test-4-3/latest/run.json").write_text(
              json.dumps(payload, indent=2) + "\n", encoding="utf-8"
          )
          PY
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add runs/test-4-3-latest.json results/test-4-3/latest
          git commit -m "results: Test 4.3 ${GITHUB_RUN_ID} (${RUN_STATUS})" || true
          git pull --rebase origin main
          git push origin HEAD:main

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: modal-moe-test-4-3-width-scaling-${{ github.run_id }}
          path: |
            results/test-4-3/latest
            test-4-3.log
          if-no-files-found: warn
          retention-days: 14
          compression-level: 9
