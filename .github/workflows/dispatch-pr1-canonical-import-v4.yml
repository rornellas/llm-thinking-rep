name: Dispatch PR1 canonical artifact import v4

on:
  issue_comment:
    types: [created]

permissions:
  actions: write
  contents: read
  pull-requests: read
  issues: read

jobs:
  dispatch:
    if: >-
      github.event.issue.pull_request &&
      github.event.issue.number == 1 &&
      github.event.comment.author_association == 'OWNER' &&
      contains(github.event.comment.body, '/dispatch-canonical-import-v4')
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch workflow on PR branch
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'import-canonical-and-version-artifacts-v4.yml',
              ref: 'agent/pre-qwen-certification-v2'
            });
