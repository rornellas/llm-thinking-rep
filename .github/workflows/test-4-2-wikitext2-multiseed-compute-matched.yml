name: Retired — Test 4.2 original workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "Superseded by test-4-2b-wikitext2-multiseed-corrected.yml."
