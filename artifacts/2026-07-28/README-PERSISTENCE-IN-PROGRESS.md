# Artifact persistence in progress

This branch is being completed under `docs/methodology/ARTIFACT_VERSIONING_POLICY.md`.

Completion requires canonical readable files, raw and aggregate results, audits, checkpoints/logs where available, delivery archives, Git bundles, handoffs, SHA-256 inventories, and deterministic reconstruction instructions to exist in Git. Sandbox-only or Actions-only copies do not count as durable delivery.
